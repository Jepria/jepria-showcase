<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.jepria.jepriashowcase</groupId>
  <artifactId>JepRiaShowcase</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>war</packaging>

  <name>JepRiaShowcase</name>
  <!-- FIXME change it to the project's website -->
  <url>http://jepria.org</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <gwt.product.name>JepRiaShowcase</gwt.product.name>
    <service.rest.product.name>jepriashowcase-service-rest</service.rest.product.name>
    <project.build.finalName>JepRiaShowcase</project.build.finalName>

    <deploy.url>http://localhost:8080/manager/text</deploy.url>
    <deploy.path>/JepRiaShowcase</deploy.path>
    <deploy.username>admin</deploy.username>
    <deploy.password>admin</deploy.password>
  </properties>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.12</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.build.finalName}</finalName>
    <pluginManagement><!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) -->
      <plugins>
        <!-- clean lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#clean_Lifecycle -->
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>3.1.0</version>
        </plugin>
        <!-- default lifecycle, jar packaging: see https://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_jar_packaging -->
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>3.1.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.8.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>2.22.1</version>
          <configuration>
            <skipTests>true</skipTests>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.0.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>2.5.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>2.8.2</version>
        </plugin>
        <!-- site lifecycle, see https://maven.apache.org/ref/current/maven-core/lifecycles.html#site_Lifecycle -->
        <plugin>
          <artifactId>maven-site-plugin</artifactId>
          <version>3.7.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>3.0.0</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.2.2</version>
        <configuration>
          <webResources>
            <resource>
              <directory>${project.basedir}/webapp/WEB-INF</directory>
              <targetPath>WEB-INF</targetPath>
              <includes>
                <include>web.xml</include>
              </includes>
            </resource>
            <resource>
              <directory>${project.build.directory}/temp-war-gwt</directory>
              <excludes>
                <exclude>WEB-INF/web.xml</exclude>
              </excludes>
            </resource>
            <resource>
              <directory>${project.build.directory}/temp-war-service-rest</directory>
              <excludes>
                <exclude>WEB-INF/web.xml</exclude>
              </excludes>
            </resource>
            <resource>
              <directory>${project.basedir}/client-react/build</directory>
              <targetPath>react</targetPath>
            </resource>
          </webResources>
          <packagingExcludes>WEB-INF/classes/</packagingExcludes>

          <archive>
            <addMavenDescriptor>false</addMavenDescriptor>
            <manifestEntries>
              <Built-By>developer</Built-By>
            </manifestEntries>
          </archive>

        </configuration>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.6.0</version>
        <executions>

          <execution>
            <id>node-install-exec</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>npm</executable>
              <workingDirectory>${project.basedir}/client-react/</workingDirectory>
              <arguments>install</arguments>
            </configuration>
          </execution>

          <execution>
            <id>node-build-exec</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>npm</executable>
              <workingDirectory>${project.basedir}/client-react/</workingDirectory>
              <arguments>
                <argument>run</argument>
                <argument>build</argument>
              </arguments>
            </configuration>
          </execution>

          <execution>
            <id>build-jepriashowcase-gwt</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>ant</executable>
              <workingDirectory>${project.basedir}/gwt</workingDirectory>
              <arguments>
                <argument>war</argument>
              </arguments>
            </configuration>
          </execution>
          <execution>
            <id>build-jepriashowcase-service-rest</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>mvn</executable>
              <workingDirectory>${project.basedir}/service-rest</workingDirectory>
              <arguments>
                <argument>package</argument>
              </arguments>
            </configuration>
          </execution>
          <execution>
            <id>clean-jepriashowcase-gwt</id>
            <phase>clean-all</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>ant</executable>
              <workingDirectory>${project.basedir}/gwt</workingDirectory>
              <arguments>
                <argument>clean-all</argument>
              </arguments>
            </configuration>
          </execution>
          <execution>
            <id>clean-jepriashowcase-service-rest</id>
            <phase>clean-all</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>mvn</executable>
              <workingDirectory>${project.basedir}/service-rest</workingDirectory>
              <arguments>
                <argument>clean</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>truezip-maven-plugin</artifactId>
        <version>1.2</version>
        <executions>
          <execution>
            <id>unpack-nested-projects</id>
            <goals>
              <goal>copy</goal>
            </goals>
            <phase>prepare-package</phase>
            <configuration>
              <filesets>
                <fileset>
                  <directory>${project.basedir}/gwt/lib/${gwt.product.name}-gwt.war</directory>
                  <outputDirectory>${project.build.directory}/temp-war-gwt</outputDirectory>
                </fileset>
                <fileset>
                  <directory>${project.build.directory}/temp-war-gwt/WEB-INF/lib/${gwt.product.name}-web.jar</directory>
                  <outputDirectory>${project.build.directory}/temp-web-jar-gwt</outputDirectory>
                </fileset>
                <fileset>
                  <directory>${project.basedir}/service-rest/target/${service.rest.product.name}.war</directory>
                  <outputDirectory>${project.build.directory}/temp-war-service-rest</outputDirectory>
                </fileset>
                <fileset>
                  <directory>${project.build.directory}/temp-war-service-rest/WEB-INF/lib/${service.rest.product.name}-web.jar</directory>
                  <outputDirectory>${project.build.directory}/temp-web-jar-service-rest</outputDirectory>
                </fileset>
              </filesets>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jepria.tools.mavenplugin</groupId>
        <artifactId>collisioncheck-maven-plugin</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <executions>
          <execution>
            <id>check-collisions-in-merged-files</id>
            <phase>prepare-package</phase>
            <configuration>
              <classPathRoots>target/temp-web-jar-gwt;target/temp-web-jar-service-rest</classPathRoots>
              <classPathRefs>gwt/lib/${gwt.product.name}-gwt.war/WEB-INF/lib/${gwt.product.name}-web.jar;service-rest/target/${service.rest.product.name}.war/WEB-INF/lib/${service.rest.product.name}-web.jar</classPathRefs>
              <filePathRoots>target/temp-war-gwt;target/temp-war-service-rest</filePathRoots>
              <filePathRefs>gwt/lib/${gwt.product.name}-gwt.war;service-rest/target/${service.rest.product.name}.war</filePathRefs>
              <onlyClassCollision>false</onlyClassCollision>
            </configuration>
            <goals>
              <goal>check-collision</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-project-artifact</id>
            <phase>package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}/lib</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.build.directory}</directory>
                  <includes>
                    <include>${project.build.finalName}.war</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jepria.tools.mavenplugin</groupId>
        <artifactId>cleanall-maven-plugin</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <extensions>true</extensions>
      </plugin>

      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <executions>
          <execution>
            <id>clean-jepriashowcase</id>
            <phase>clean-all</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <filesets>
            <fileset>
              <directory>${project.basedir}/lib</directory>
              <includes>
                <include>**/*</include>
              </includes>
              <followSymlinks>false</followSymlinks>
            </fileset>
          </filesets>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.tomcat.maven</groupId>
        <artifactId>tomcat7-maven-plugin</artifactId>
        <version>2.2</version>
        <configuration>
          <update>true</update>
          <url>${deploy.url}</url>
          <username>${deploy.username}</username>
          <password>${deploy.password}</password>
          <path>${deploy.path}</path>
          <failOnError>true</failOnError>
        </configuration>
      </plugin>

    </plugins>
  </build>

</project>
